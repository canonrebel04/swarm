# Plan 02-2: Define Support Roles

## Objective
Define the support role prompts (`reviewer`, `merger`, `monitor`) in `src/agents/definitions/` to handle quality control, conflict resolution, and health monitoring.

## Tasks
1. **Define Reviewer**: Create `src/agents/definitions/reviewer.md` with auditing and feedback responsibilities.
2. **Define Merger**: Create `src/agents/definitions/merger.md` with git conflict resolution and branch finalization logic.
3. **Define Monitor**: Create `src/agents/definitions/monitor.md` with health check and role drift detection logic.

## Verification
- [ ] `src/agents/definitions/reviewer.md` exists and contains correct role definition.
- [ ] `src/agents/definitions/merger.md` exists and contains correct role definition.
- [ ] `src/agents/definitions/monitor.md` exists and contains correct role definition.
- [ ] All files follow the standard role definition template.
