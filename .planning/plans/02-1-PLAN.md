# Plan 02-1: Define Orchestration Roles

## Objective
Define the core orchestration role prompts (`orchestrator`, `coordinator`, `supervisor`, `lead`) in `src/agents/definitions/` to provide clear identity and mission for these high-level agents.

## Tasks
1. **Define Orchestrator**: Create `src/agents/definitions/orchestrator.md` with multi-project coordination and top-level goal management logic.
2. **Define Coordinator**: Create `src/agents/definitions/coordinator.md` with task decomposition and dispatcher logic.
3. **Define Supervisor**: Create `src/agents/definitions/supervisor.md` with fleet oversight and intervention capabilities.
4. **Define Lead**: Create `src/agents/definitions/lead.md` with team-level coordination and workstream management responsibilities.

## Verification
- [ ] `src/agents/definitions/orchestrator.md` exists and contains correct role definition.
- [ ] `src/agents/definitions/coordinator.md` exists and contains correct role definition.
- [ ] `src/agents/definitions/supervisor.md` exists and contains correct role definition.
- [ ] `src/agents/definitions/lead.md` exists and contains correct role definition.
- [ ] All files follow the standard role definition template.
